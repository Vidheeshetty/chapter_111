name: chapter_11
description: "Chapter 11 Flutter project with Firebase Authentication."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Firebase dependencies - STABLE VERSIONS
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  google_sign_in: ^6.3.0

  # UI and State Management
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  connectivity_plus: ^5.0.2
  internet_connection_checker: ^1.0.0+1
  pin_code_fields: ^8.0.1
  logger: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Uncomment and add font files if you want custom fonts
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: fonts/Poppins-Regular.ttf
  #       - asset: fonts/Poppins-Medium.ttf
  #         weight: 500
  #       - asset: fonts/Poppins-SemiBold.ttf
  #         weight: 600

  # Uncomment if you have custom assets
  # assets:
  #   - assets/images/
  #   - assets/icons/